# QUSAD.prod - Personal Portfolio Website

Современный одностраничный сайт-визитка для Flutter разработчика с поддержкой локализации (русский/английский).

## 🚀 Особенности

- ✨ Современный дизайн с плавными анимациями
- 🌍 Двуязычная поддержка (Русский/Английский)
- 📱 Полностью адаптивный дизайн
- 🎨 Единая стилистика с градиентами и анимациями
- 📋 Интерактивные карточки проектов с возможностью разворачивания
- 💼 Секция опыта работы с временной линией
- 🎓 Секция образования
- 📧 Всегда видимые контакты для связи

## 🛠 Технологии

- React 19.2.0
- TypeScript
- Vite
- CSS3 с анимациями

## 📦 Установка

```bash
npm install
```

## 🏃 Запуск

```bash
# Режим разработки
npm run dev

# Сборка для продакшена
npm run build

# Просмотр продакшен сборки
npm run preview
```

## 📁 Структура проекта

```
src/
├── components/          # React компоненты
│   ├── Hero.tsx        # Главная секция с именем
│   ├── Projects.tsx    # Секция проектов
│   ├── Experience.tsx  # Опыт работы
│   ├── Education.tsx   # Образование
│   ├── Contacts.tsx    # Контакты (всегда видны)
│   └── LanguageSwitcher.tsx  # Переключатель языка
├── context/            # React Context
│   └── LanguageContext.tsx   # Контекст локализации
├── data/              # Данные сайта
│   ├── ru.ts          # Данные на русском
│   └── en.ts          # Данные на английском
├── types/             # TypeScript типы
│   └── index.ts
├── utils/             # Утилиты
│   └── firebase.ts    # Интеграция с Firebase (опционально)
└── hooks/             # Кастомные хуки
    └── useScrollAnimation.ts
```

## 📝 Редактирование данных

Все данные сайта хранятся в файлах `src/data/ru.ts` и `src/data/en.ts`.

### Добавление аватарки

В файлах данных добавьте путь к изображению:

```typescript
personalInfo: {
  avatar: '/path/to/avatar.jpg', // или URL
  // ...
}
```

### Добавление проектов

Проекты добавляются в массив `projects` внутри каждого опыта работы:

```typescript
projects: [
  {
    id: 'project-id',
    title: {
      ru: 'Название проекта',
      en: 'Project Title',
    },
    description: {
      ru: 'Краткое описание',
      en: 'Short description',
    },
    detailedDescription: {
      ru: 'Подробное описание',
      en: 'Detailed description',
    },
    technologies: ['Flutter', 'Dart'],
    links: [
      {
        label: 'GitHub',
        url: 'https://github.com/...',
      },
    ],
  },
]
```

## 🔥 Интеграция с Firebase (опционально)

Для загрузки данных из Firebase:

1. Установите Firebase:
```bash
npm install firebase
```

2. Настройте конфигурацию в `src/utils/firebase.ts`

3. Обновите `src/context/LanguageContext.tsx` для использования Firebase вместо файлов

## 🎨 Кастомизация

### Цветовая схема

Основные цвета можно изменить в CSS файлах компонентов:
- Градиент: `linear-gradient(135deg, #667eea 0%, #764ba2 100%)`
- Основной цвет: `#667eea`

### Анимации

Анимации настраиваются в CSS файлах компонентов через `@keyframes` и `transition`.

## 📱 Адаптивность

Сайт полностью адаптирован для:
- Десктоп (1920px+)
- Планшет (768px - 1024px)
- Мобильные устройства (< 768px)

## 🔄 Добавление дополнительных блоков

Для добавления новых секций:

1. Создайте компонент в `src/components/`
2. Добавьте данные в `src/data/ru.ts` и `src/data/en.ts`
3. Импортируйте и добавьте компонент в `src/App.tsx`

## 📄 Лицензия

MIT

## 👤 Автор

QUSAD.prod - Дмитрий Островский
